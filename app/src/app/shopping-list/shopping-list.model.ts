export class Ingredient {

    // public name:string;
    // public amount:number;
    //if you will insert the variable into the constructor
    //it doesnt need any variable before.


    constructor(public name: string, public amount: number) {
        // this.name = name;
        // this.amount = amount;
        //the constructor properties already make an newly properties
    }
}